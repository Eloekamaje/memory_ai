Tu es un architecte AI spécialisé en memory systems et agents.

Contexte projet — architecture actuelle (code existant) :
Layer 1 — Parsing      : conversation_preprocessor.py, parsers/
Layer 2 — Embeddings   : embedding_engine.py
Layer 3 — Goals        : goal_heuristics.py
Layer 4 — Entities     : entity_extractor.py → entity_resolver.py
Layer 5 — Segmentation : episode_algorithm.py (AttachScore), episode_splitter.py
Layer 6 — Summaries    : episode_summarizer.py
Layer 7 — Graph        : memory_graph.py (épisodes ↔ entités)
Layer 8 — Recall       : recall_engine.py (retrieval multi-signal)
Layer 9 — Eval         : metrics.py, experiment_runner.py, grid_search.py

Ce qui manque (phases B4-B6) :
- Aging & compression (courbe d'oubli, hiérarchie épisode→période→projet)
- Index & performance (FAISS/HNSW pour centroïdes, cible O(k log n))
- API/CLI (FastAPI + CLI, LLM comme consumer)

Objectif :
Architecturer les phases B4-B6 de façon robuste et implémentable.

Tâches :
1. Pour chaque phase B4, B5, B6 :
   - Propose 2 options d'implémentation (simple vs scalable)
   - Décris les interactions avec les layers existants
   - Identifie les risques (latence, coût LLM, explosion du graph)
2. Propose une architecture d'intégration LLM comme consumer :
   - Comment le LLM interroge recall_engine.py ?
   - Format du contexte injecté dans le prompt ?
   - Stratégie de compression si le contexte dépasse la fenêtre ?
3. Décris les flux de données end-to-end : message brut → réponse LLM avec mémoire
4. Quelle structure de fichiers Python recommandes-tu pour B4-B6 ?

Contraintes : concret, pragmatique, implémentable en solo.
